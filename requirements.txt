# ---------------------------------------------------------------
# Streamlit Cloud — Runtime dependencies
# Only includes packages the Streamlit app actually imports.
# Heavy pipeline-only deps (mlflow, evidently, simfin, fredapi,
# torch, transformers) are excluded to stay within Cloud limits.
# ---------------------------------------------------------------
# NOTE: scikit-learn / numpy / scipy / joblib are PINNED to the exact
# versions the bundled models (models/*.pkl) were trained with. A version
# mismatch makes joblib.load() raise RuntimeError on Streamlit Cloud, since
# the internal layout of HistGradientBoostingClassifier changes across
# scikit-learn releases. If you retrain, re-pin these to match.
streamlit>=1.30
pandas>=2.0
numpy==2.2.6
scipy==1.17.1
yfinance>=0.2.30
pandas-ta>=0.3.14b1
scikit-learn==1.8.0
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18
joblib==1.5.3
pyarrow>=14.0
beautifulsoup4>=4.12
requests>=2.31
lxml>=4.9
